/** Rótulos dos métodos de entrada de crescimento (compartilhados entre componentes). */
export const METHOD_LABELS = {
    period: 'Por período',
    avg: 'Taxa média',
    yearly: 'Ano-a-ano',
};

/** Tipos de driver. */
export const DRIVER_TYPES = ['Físico', 'Financeiro', 'Operacional'];
