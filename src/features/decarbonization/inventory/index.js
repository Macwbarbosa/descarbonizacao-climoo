export { default as InventoryPage } from './InventoryPage';
// Saída reutilizável (Metas/Cenários): totais por escopo a partir do inventário.
export { aggregateByScope } from './utils/inventoryAggregate';
